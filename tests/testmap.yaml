file : 
  - name : Files.Name
  - block : Files.Block
  - createdate : Files.CreateDate

dataset :
  - createdate : ProcessedDataset.CreateDate
  - name : Files.Path

primds : 
  - name : PrimaryDataset.Name
  - startdate : PrimaryDataset.StartDate

procds :
  - name : ProcessedDataset.Name

block :
  - name : Block.Name
  - size : Block.BlockSize

algo :
  - name : ProcAlgo.Algorithm

phygrp :
  - name : ProcessedDataset.PhysicsGroup

migrate :
  - name : MigrationBlocks.BlockID
  - req : MigrationRequest.URL
